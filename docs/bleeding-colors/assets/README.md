Optional developer-attached assets for `bleeding-colors`:

- `paper-grain.png` (grayscale texture, suggested 2048x2048)
- `sun-hum.mp3` (ambient loop, suggested 10-30 seconds)

The piece runs without these files.
