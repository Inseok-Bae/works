# Review Ledger

This file is a repo-level memory of curator reviews. Update it through `$curator-reflection-loop` with `mode=memory-sync`.

## Entries

### 2026-02-11 - `violet-cloud-and-sharp-boundaries`
- Preserve:
  - derived relationship metrics can be useful when they remain indirect
- Adjust:
  - expose derived metrics with clearer labels so the viewer knows they are relations, not characters
- Reusable lesson:
  - if a metric is shown in UI, its semantic type should stay legible

### 2026-02-23 - `bleeding-colors`
- Preserve:
  - delayed consequence and residue-based causality
- Adjust:
  - time-driven pieces need a stronger felt ending than an endlessly running field
  - multiple gestures can weaken a plot that wants one accidental trigger
  - metrics-heavy HUD can flatten a thought that needs atmosphere, field, and afterimage
- Reusable lesson:
  - for one-way emotional or entropic pieces, specify loop, end latch, and restart path early

## Sync Rule

- Add a new entry only after a curator review or session has been compressed into a `maturity-delta`.
- Keep entries short and reusable.

