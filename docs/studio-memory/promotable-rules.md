# Promotable Rules

Candidate rules live here before they are promoted into `AGENTS.md` or skill files.

## Candidates

- `preflight-review-before-plan`
  - Evidence:
    - `bleeding-colors` review revealed major structural changes before implementation details should harden
  - Promotion test:
    - repeat this need in at least one more project

- `time-based-piece-needs-end-latch`
  - Evidence:
    - `bleeding-colors` needed explicit closure, white ending, and restart path
  - Promotion test:
    - promote if another time/entropy piece needs the same intervention

- `single-trigger-autonomous-loop-is-valid`
  - Evidence:
    - `bleeding-colors` moved closer to the THOUGHT when interaction was reduced to an accidental touch
  - Promotion test:
    - promote if at least one more piece benefits from trigger-only interaction

- `derived-metrics-need-legible-labels`
  - Evidence:
    - `violet-cloud-and-sharp-boundaries` exposed `motherDistance` ambiguously until clarified
  - Promotion test:
    - promote if another work surfaces relation-derived metrics in UI

